programa teste6
    a, b is int;
    result is int;
    a,x is float;
    c is char;
begin
    a = 12a;
    x = 12.;
    c = 'b';
    read (a);
    read (b);
    read (c)
    result = (a*b + 1) / (c+2);
    write {Resultado: };
    write (result);
end.